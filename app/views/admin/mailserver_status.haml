- @crumbtrail = link_to_admin_home
- @title = 'Mailserver Status'

%h3.padded_subheading Server ping test

-if params[:commit] == 'Ping server'
  %pre.column_content
    = @ping_test
    %br

- form_tag(mailserver_status_url, :method => :get) do
  = submit_tag 'Ping server'

%p.text
  = '&nbsp;'

%h3.padded_subheading FastMail.FM recent status updates

%div.text
  = @feed

%p.text
  = '&nbsp;'
%p.text
  = link_to 'Admin home', admin_url