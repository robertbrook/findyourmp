%html{:lang=>"en-GB"}
  %head
    %title= @title ? "#{@title} (Find Your MP)" : "Search (Find Your MP)"
    = stylesheet_link_tag 'screen', :media => 'screen'
  %body{:id=>"findyourmp-parliament-uk"}
    #header
      %h1= @title || "Find Your MP"
    #content= yield
    #footer
      #about= "The Find Your MP service recognises " + number_with_delimiter(@postcode_count.to_s) + " UK post codes and " + @constituency_count.to_s + " Westminster constituencies."
      #help If you have any problems using this service please <a href="http://www.parliament.uk/directories/hcio/findyourmp_help.cfm">look at the help pages</a> or contact the <a href="http://www.parliament.uk/directories/hcio.cfm">House of Commons Information Office</a>.
      #copyright Source: NSPD 2008, National Statistics. Crown copyright material is reproduced with the permission of the Controller of HMSO and Queen’s Printer for Scotland.
      #admin-button= button_to "Enable #{@is_admin ? 'STANDARD USER functions' : 'ADMINISTRATOR functions'}" , :action => :toggle_admin, :controller => 'application'
      - if @postcode_format_links
        %p{:style=>'float:right'}= "Export data as " + @postcode_format_links
