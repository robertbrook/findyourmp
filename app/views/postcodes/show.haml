- if @postcode
  - @title = @postcode.code_with_space
  - if @constituency
    - name = @postcode ? link_to(h(@constituency.name), :controller => "constituencies", :action => "show", :only_path => false, :id => @constituency) : h(@constituency.name)
    %p.text= "The postcode <strong>#{@postcode.code_with_space}</strong> is in the Westminster constituency of #{name}."
    = render :partial => 'constituencies/member'
  - else
    %p.text No constituency found.
- elsif @postcodes
  - @title = @search_term.upcase
  %h3.subheading Search Results
  %p.text You appear to have entered a partial postcode which covers more than one constituency - for a more accurate result, please enter your full postcode
  %h4= pluralize(@postcodes.size, 'Constituencies')
  = render :partial => 'constituencies/constituency_match', :collection => @postcodes
- @crumbtrail = @title