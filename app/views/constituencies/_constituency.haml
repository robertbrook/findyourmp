%p<
  - name = @postcode ? link_to(h(constituency.name), constituency_path(constituency)) : h(constituency.name)
  = "CONSTITUENCY<br/><strong>#{name}</strong>"
%p<
  - unless constituency.member_name.blank?
    = "MEMBER<br/><strong>#{h constituency.member_name}</strong>"
  - else
    = "NO RECORDED MEMBER"